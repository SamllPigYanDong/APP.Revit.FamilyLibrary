<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Revit.Application.Views.ProjectViews"
             x:Class="Revit.Application.Views.ProjectViews.ProjectFileManageView"
             mc:Ignorable="d"
             Height="auto"
             Width="auto"
             HorizontalAlignment="Stretch"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TreeView />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="上传文件"></Button>
                <Button Content="新建文件夹"></Button>
                <Button Content="下载"></Button>
                <Button Content="分享"></Button>
                <Button Content="更多"></Button>
            </StackPanel>
            <DataGrid Grid.Row="1"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn>
                        <DataGridCheckBoxColumn.Header>
                            <CheckBox />
                        </DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="文件名称"
                                        Width="*" />
                    <DataGridTextColumn Header="上传人员"
                                        Width="*" />
                    <DataGridTextColumn Header="文件大小"
                                        Width="*" />
                    <DataGridTextColumn Header="上传时间"
                                        Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
